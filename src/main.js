import { createApp } from 'vue'
import App from './App.vue'

import './css/styles.css'/*es solo un punto . cuando es al mismo nivel, y 2 puntos cuando  sube al nivel padre*/ 
createApp(App).mount('#app')
